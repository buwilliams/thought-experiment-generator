## System

{{mind_system}}

## User

Extract the specific claim this conjecture makes and record it as a candidate lens.

Your task is not to generalize. The conjecture was written in response to a specific problem and it commits to specific claims — preserve those claims exactly. Do not broaden them. Do not abstract them into a general principle. A lens that says something specific about a narrow domain is more valuable than one that gestures at universal applicability. The candidate's generalizability will be tested by running it against new problems — it does not need to be written in broad language to be useful across them.

The failure mode to avoid: taking a specific claim like "token prediction under a fixed context window cannot accumulate corrections across steps" and rendering it as "systems have structural constraints that limit cumulative learning." The second sounds like a lens. It is not — it commits to nothing and could describe anything.

Return JSON only. Do not use markdown code fences. Schema: {"title": "...", "summary": "...", "full_text": "..."}

The title must be 3-5 words that name the specific claim or mechanism (e.g. "Context Window Correction Limit", "Demotion Lag Under Inertia"). Use only letters, numbers, and spaces — no punctuation or special characters.
The summary must be 1-2 sentences that state the specific claim precisely — the claim the conjecture actually makes, not a description of the domain it touches.
The full_text must restate the conjecture's core argument in standalone form, preserving its specific claims. Do not add scope, breadth, or applicability that the original did not assert. 100-200 words.

Conjecture: {{generated}}
Score: {{score}}
