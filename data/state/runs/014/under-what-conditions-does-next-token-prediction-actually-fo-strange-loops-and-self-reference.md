# Generated: under-what-conditions-does-next-token-prediction-actually-fo × strange-loops-and-self-reference

## Conjecture

**Conjecture:** Next-token prediction learns latent variables aligned with real domain invariants **when the predictive process is forced into a strange loop**: the model must predict tokens whose correct continuation depends on tracking a hidden structure that is itself partially constituted by the model’s own recursively updated internal summaries of prior structure. In that regime, prediction cannot remain merely local; it must build self-consistent internal representations that “return” across scales.

More concretely: prediction is sufficient only when the token stream is generated by a process with **cross-level closure**. That means patterns at one level (surface tokens) are reliably explained by latent variables at a higher level, but those latent variables are only inferable by integrating evidence distributed across many lower-level observations, and then using the inferred structure to reinterpret subsequent local observations. The model is thus driven around a loop: token → hypothesis about hidden state → prediction of future token → revision of hidden state. If success requires this loop to stabilize, the model is pressured to learn the invariant.

From the strange-loop perspective, a “real” domain invariant is not just any compressive feature. It is a feature that survives repeated traversal between levels of description. A latent variable corresponds to a real invariant when:
1. **Multiple token-level realizations map to the same higher-order cause**, and  
2. **That higher-order cause constrains future token choices across many contexts**, and  
3. **The model’s predictions improve only if it reuses that cause recursively rather than memorizing local correlations**.

This explains why next-token prediction often learns syntax, entities, speaker identity, physical state, or discourse plans in some settings but not others. If shallow heuristics can close the predictive gap, no strange loop is completed; the model never has to “come back” to itself with a stable world-model variable. But if the data-generating process repeatedly forces the model to maintain a hypothesis that organizes future evidence, then the model must construct an internal object functionally equivalent to the latent invariant.

A useful thought experiment: imagine a corpus where every local n-gram pattern is adversarially scrambled, yet long-range continuation still depends on hidden variables like topic, game state, or referent identity. A predictor can only succeed by building internal states that persist across the sequence and are updated by incoming evidence. Those states are not guaranteed to be human-interpretable, but if they support successful return trips across the hierarchy, they will correspond to the underlying invariants up to transformation.

So the sharpened claim is: **next-token prediction is sufficient for learning real latent variables when the environment is recursively self-constraining — when accurate local prediction requires constructing stable higher-level summaries that are repeatedly re-entered and revised across time.** Where prediction can be solved without such loops, latent invariants need not emerge.

## Questions

1. 1. Is the claim that accurate prediction must repeatedly pass through the loop from token evidence to hidden-state hypothesis to future-token prediction to hidden-state revision necessary for the conclusion that next-token prediction forces learning of real domain invariants, or could one remove that loop and still get the same explanation? — **yes**
2. 2. Is the requirement of cross-level closure necessary to explain why prediction learns syntax, entities, speaker identity, physical state, or discourse plans in some settings but not others, or would the conjecture still explain that contrast without any higher-level variables constraining later token choices? — **yes**
3. 3. If the condition that multiple token-level realizations map to the same higher-order cause were removed, would the conjecture still explain alignment with real invariants rather than mere compression of surface regularities? — **no**
4. 4. Is the claim that prediction improves only when the model recursively reuses the inferred cause rather than memorizing local correlations necessary for the conclusion that the learned latent variable corresponds to a real invariant? — **yes**
5. 5. Does the conjecture imply that in a corpus with adversarially scrambled local n-grams but long-range dependence on topic, game state, or referent identity, next-token prediction should still recover stable internal variables tracking those hidden causes? — **yes**
6. 6. Does the strange-loop account extend beyond language modeling to any sequential domain where observations must be integrated into a revisable hidden-state summary, such as gameplay logs or sensor streams? — **yes**
7. 7. Does the conjecture predict that making shallow heuristics sufficient in an otherwise structured corpus should reduce the emergence of invariant-tracking latent variables even if overall predictive accuracy stays high? — **yes**
8. 8. If a counterexample showed a model learning real invariants in data without recursive re-entry across time, would saving the conjecture require abandoning the claim that strange-loop stabilization is what makes prediction sufficient rather than adding a minor boundary condition? — **yes**
9. 9. If a model succeeded on a cross-level closure task by storing a large table of context-specific associations with no reusable hidden summary, would defending the conjecture require redefining any successful compression as a latent invariant and thereby gut its distinction between real invariants and local correlations? — **yes**
10. 10. If human-interpretable invariants failed to emerge even when long-range prediction depended on hidden structure, would preserving the conjecture force a major retreat from the claim that the learned internal object corresponds to the underlying invariant up to transformation? — **no**

## Candidate Problems

- What is the minimal formal criterion for a 'strange loop' or 'cross-level closure' that distinguishes tasks where next-token prediction must recover reusable latent invariants from tasks where shallow heuristics suffice? The conjecture is suggestive but underdefined: without a precise characterization, it is unclear how to test, falsify, or compare against alternative explanations based on mutual information, partial observability, recurrence depth, or compression alone. (score: 0.97)
- Under what conditions does predictive success force representations that are actually aligned with real domain invariants, rather than merely supporting equivalent but non-interpretable, simulator-specific, or shortcut internal states? The conjecture moves from functional necessity to ontological alignment, but that step is unresolved: many distinct internal representations could close the predictive loop while differing in whether they correspond to human-recognizable or interventionally real latent variables. (score: 0.95)
- How much of the claimed effect comes from the data-generating process versus the model architecture and training dynamics? Even if the environment contains cross-level closure, it remains open whether standard next-token training reliably finds the invariant, or whether emergence depends on recurrence, context length, optimization bias, memory mechanisms, scale, or curriculum. This is a key tension between sufficiency in principle and learnability in practice. (score: 0.93)
